<table id='table-2'>
	<thead>
		<th colspan='2'>Hechizo</th>
		<th>Descripción</th>
	</thead>
	<tbody>
		<% sangres.each do |s|%>
			<tr style=<%= s.color %>>				
				<td><%= erb :'display/spell', :locals => {:spell => s} %></td>
				<td><%= s.name.capitalize %></td>
				<td><%= s.efecto %></td>
			</tr>
		<% end %>
	</tbody>
</table>