<table>
	<thead>
		<th colspan='2'>Execración</th>
		<th>Efecto</th>
	</thead>
	<tbody>
		<% execraciones.each do |e|%>
			<tr style=<%= e.color %>>
				<td><%= erb :'display/spell', locals: { spell: e } %></td>
				<td><%= e.name.capitalize %></td>
				<td><%= e.efecto %></td>
			</tr>
		<% end %>
	</tbody>
</table>