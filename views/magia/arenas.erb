<table id='tresp'>
	<thead>
		<th colspan='2'>Hechizo</th>
		<th>Descripción</th>
	</thead>
	<tbody>
		<% arenas.each do |a| %>
			<tr style=<%= a.color %>>
				<td><%= erb :'display/spell', locals: { spell: a } %></td>
				<td><%= a.name.capitalize %></td>
				<td><%= a.efecto %></td>
			</tr>
		<% end %>
	</tbody>
</table>