<br/>
<% r = renegado(params[:param].to_i) %>
<table>
	<thead>
		<tr><% resistencias.each do |r| %>	
				<th><%= erb :'display/display', locals: {
						title:       r['nombre'], 
						path:        "'../images/elements/#{r['nombre']}.png'",
						description: r['description'] } %>
				</th>
			<% end %>
		</tr>
	</thead>
	<tbody>
		<tr><% resistencias.each do |res| %>
				<td>+<%= r.resistencia(res['nombre']) %></td>
			<% end%>
		</tr>
	</tbody>
</table>