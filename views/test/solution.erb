<section>
	<header>
		<h2>Tu héroe preferente es...</h2>
	</header>
	<link rel="stylesheet" href="../css/bravo.css" />
	<table id='tresp'>
		<tbody>
			<% quiensoy(params[:param]).each do |k,v| %>
				<tr>
					<td>
						<% img_path = "'../images/personajes/#{k}.png'"  %>
						<img src=<%= img_path %> alt=<%= k %> , id='icon' />
					</td>
					<td><%= k %></td>
					<td><h2><%= v %></h2></td>
				</tr>
			<% end %>
		</tbody>
	</table><br>

<% #Tu código es <%= quiensoy(params[:param]) %>
</section>