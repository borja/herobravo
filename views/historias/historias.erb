<!DOCTYPE HTML>
<html>
	<% h = hero(params[:param].to_i) %>
	<head><%= erb :head, :locals => {:title => "HeroSheet: historia de #{h.name}"} %></head>
	<body class="subpage">
		<div id="header-wrapper">
			<%= erb :header, :locals => { :title => h.name } %>
		</div>
			<div id="content-wrapper">
				<div id="content">
					<div class="container">
						<div class="row">
							<div class="3u">
								<%= erb :'ficha/izquierda/izquierda' %>
							</div>
							<div class="6u important(collapse)">
								<%= erb :"historias/relatos#{h.historia}/#{h.id}" %>
							</div>
							<div class="3u">
								<%= erb :'ficha/derecha/derecha' %>
							</div>
						</div>
					</div>
				</div>
			</div>
		<%= erb :'footer/footer' %>	
	</body>
</html>